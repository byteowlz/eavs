upstream:
  default:
    type: openai
    api_key: "env:OPENAI_API_KEY" # This will need to be parsed or substituted
    base_url: "https://api.openai.com/v1"

logging:
  sink: stdout

analysis:
  enabled: true
  broadcast_channel_size: 1024
